import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chetz.Foodie',
  appName: 'FoodieApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
